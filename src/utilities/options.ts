//import { CookieOptions } from "express";

const verboseDebugging: boolean = true;

/*const cookieOptions: CookieOptions = {
    httpOnly: true,
    signed: true, 
    secure: true, 
    sameSite: 'none' 
};*/

export default {
    verboseDebugging,
//    cookieOptions,
}